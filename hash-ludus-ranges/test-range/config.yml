ludus:
  - vm_name: "{{ range_id }}-test-windows-11"
    hostname: "{{ range_id }}-win-test"
    template: windows-11-enterprise-debloated
    vlan: 20
    ip_last_octet: 2
    ram_gb: 4
    cpus: 2
    roles:
      - windows_post_config
    windows:
      sysprep: true
